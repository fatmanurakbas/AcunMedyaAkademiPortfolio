@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="tr">
@Html.Action("PartialHead","Default")

<body data-spy="scroll" data-target=".site-navbar-target" data-offset="300">
    @Html.Action("PartialNavbar", "Default")

    @Html.Action("PartialFeature", "Default")

    @Html.Action("PartialStatistic", "Default")



    <section class="ftco-about ftco-section ftco-no-pt ftco-no-pb" id="about-section">
        <div class="container">
            <div class="row d-flex no-gutters">
                <div class="col-md-6 col-lg-5 d-flex">
                    <div class="img-about img d-flex align-items-stretch">
                        <div class="overlay"></div>
                        <div class="img d-flex align-self-stretch align-items-center" style="background-image: url(/Template/clyde-master/images/About.jpg);">
                        </div>
                    </div>
                </div>
                <div class="col-md-6 col-lg-7 pl-md-4 pl-lg-5 py-5">
                    <div class="py-md-5">
                        <div class="row justify-content-start pb-3">
                            <div class="col-md-12 heading-section ftco-animate">

                                @Html.Action("PartialAbout", "Default")

                                @Html.Action("PartialProfile", "Default")

                            </div>
                            @Html.Action("PartialHobby", "Default")
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    @Html.Action("PartialSkill", "Default")

    @Html.Action("PartialServices", "Default")



    <section class="ftco-hireme">
        <div class="container">
            <div class="row justify-content-between">
                <div class="col-md-8 col-lg-8 d-flex align-items-center">
                    <div class="w-100 py-4">
                        <h2>Aklınızda bir proje varsa bunu tüm dünya duymalı.</h2>
                        <p>Her büyük proje, küçük bir düşünceden doğar. Sizi farklı kılan o fikri keşfetmeye ve büyütmeye ne dersiniz?</p>
                        <p class="mb-0"><a href="#contact-section" class="btn btn-white py-3 px-4">Bizimle İletişime Geçin.</a></p>
                    </div>
                </div>
                <div class="col-md-4 col-lg-4 d-flex align-items-end">
                    <img src="images/author.png" class="img-fluid" alt="">
                </div>
            </div>
        </div>
    </section>

    @Html.Action("PartialProject", "Default")

    @Html.Action("PartialTestimonial", "Default")




    <section class="ftco-section contact-section ftco-no-pb" id="contact-section">
        <div class="container">
            <div class="row justify-content-center mb-5 pb-3">
                <div class="col-md-7 heading-section text-center ftco-animate">
                    <span class="subheading">İLETİŞİM</span>
                    <h2 class="mb-4">Nasıl bir projeniz var?</h2>
                    <p>Bizimle iletişime geçerek projelerinizle ilgili tüm detayları ve ihtiyaçlarınızı paylaşabilirsiniz. Sizlere en iyi çözümleri sunmak için buradayız.</p>
                </div>
            </div>

            <div class="row block-9">
                @Html.Action("PartialContact", "Default")

                @Html.Action("PartialAdress", "Default")

            </div>
        </div>
    </section>

    @Html.Action("PartialFooter", "Default")




    <!-- loader -->
    <div id="ftco-loader" class="show fullscreen"><svg class="circular" width="48px" height="48px"><circle class="path-bg" cx="24" cy="24" r="22" fill="none" stroke-width="4" stroke="#eeeeee" /><circle class="path" cx="24" cy="24" r="22" fill="none" stroke-width="4" stroke-miterlimit="10" stroke="#F96D00" /></svg></div>

    @Html.Action("PartialScript", "Default")


</body>
</html>